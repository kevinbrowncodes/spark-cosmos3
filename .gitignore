# ── Model weights — NEVER commit (Cosmos3-Nano is ~33 GB) ──────────────
*.safetensors
*.pt
*.pth
*.bin
*.ckpt
*.gguf
*.onnx
*.distcp
models/
weights/
checkpoints/
huggingface/
.cache/

# ── Job logs (request/upsampler/cosmos chain, written by the gateway) ───
data/logs/jobs/

# ── Generated media ─────────────────────────────────────────────────────
output/
outputs/
*.mp4
*.webm
*.avi
*.mov

# ── Large/third-party documents (keep local; distill into .md instead) ──
docs/*.pdf

# ── Secrets ──────────────────────────────────────────────────────────────
.env
*.token
hf_token*

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ── Editor / OS ──────────────────────────────────────────────────────────
*.code-workspace
.vscode/
.idea/
.DS_Store

# coverage data
.coverage
.coverage.*
